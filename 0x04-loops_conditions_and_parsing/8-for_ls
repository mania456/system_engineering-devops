#!/usr/bin/env bash
# This script lists non-hidden files in the current directory, displaying only the part of each filename after the first dash (-), using a for loop.

for file in *[^.]*; do
    if [[ -f "$file" && ! "$file" =~ ^\. ]]; then
        echo "${file#*-}"
    fi
done
